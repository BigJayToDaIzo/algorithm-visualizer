import { dijkstra, getNodesInShortestPathOrder } from './dijkstra';

describe('dijkstra', () => {
  it('should expose a function', () => {
		expect(dijkstra).toBeDefined();
	});
  
  xit('dijkstra should return expected output', () => {
    // const retValue = dijkstra(grid,startNode,finishNode);
    expect(false).toBeTruthy();
  });
});

describe('getNodesInShortestPathOrder', () => {
  it('should expose a function', () => {
		expect(getNodesInShortestPathOrder).toBeDefined();
	});
  
  xit('getNodesInShortestPathOrder should return expected output', () => {
    // const retValue = getNodesInShortestPathOrder(finishNode);
    expect(false).toBeTruthy();
  });
});